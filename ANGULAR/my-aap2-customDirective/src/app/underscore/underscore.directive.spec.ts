import { UnderscoreDirective } from './underscore.directive';

describe('UnderscoreDirective', () => {
  it('should create an instance', () => {
    const directive = new UnderscoreDirective();
    expect(directive).toBeTruthy();
  });
});
